import React, { ReactElement } from 'react';


import './App.scss';

export default class App extends React.Component {
	render = (): ReactElement => {
		return (
			<div className="App">
			</div>
		);
	};
}
